---
layout: base.njk
---

<!-- Navigation -->
<nav>
    <a href="/" class="logo">Konan David</a>
    <div class="nav-right">
        <ul class="nav-links">
            <li><a href="#work" data-i18n="work">Work</a></li>
            <li><a href="#about" data-i18n="about">About</a></li>
        </ul>
        <div class="lang-toggle">
            <button data-lang="en" class="active">EN</button>
            <button data-lang="fr">FR</button>
        </div>
        <a href="#contact" class="nav-contact">
            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"></path>
                <polyline points="22,6 12,13 2,6"></polyline>
            </svg>
            <span data-i18n="contact">Contact</span>
        </a>
    </div>
    <button class="mobile-menu-btn" aria-label="Menu">
        <span></span>
        <span></span>
        <span></span>
    </button>
</nav>

<!-- Mobile Menu -->
<div class="mobile-menu">
    <a href="#work" data-i18n="work">Work</a>
    <a href="#about" data-i18n="about">About</a>
    <a href="#contact" data-i18n="contact">Contact</a>
    <div class="lang-toggle">
        <button data-lang="en" class="active">EN</button>
        <button data-lang="fr">FR</button>
    </div>
</div>

<!-- Hero Section -->
<section class="hero" id="hero">
    <div class="hero-content">
        <div class="hero-showreel" id="hero-showreel">
            <iframe 
                id="hero-video"
                src="{{ site.vimeoShowreel }}?background=1&autoplay=1&loop=1&byline=0&title=0&muted=1"
                allow="autoplay; fullscreen; picture-in-picture"
                allowfullscreen>
            </iframe>
        </div>
        <div class="hero-text" id="hero-text">
            <h1>
                <span class="title-block">Choreography</span>
                <span class="separator">+</span>
                <span class="title-block">Creative & Movement Direction</span>
            </h1>
        </div>
        <div class="scroll-indicator">
            <div class="scroll-line"></div>
        </div>
    </div>
</section>

<!-- Work Section -->
<section class="work-section" id="work">
    <div class="section-header">
        <h2 data-i18n="selectedWork">Selected Work</h2>
        <div class="drag-indicator">
            <span data-i18n="dragToExplore">Drag to explore</span>
            <div class="drag-line"></div>
        </div>
    </div>
    <div class="projects-wrapper">
        <div class="projects-container">
            {%- for project in collections.projects -%}
            <article class="project-card" {% if project.data.video %}data-video="{{ project.data.video }}"{% endif %}>
                <img src="{{ project.data.image }}" alt="{{ project.data.title }}" loading="lazy">
                {% if project.data.video %}<div class="project-play"></div>{% endif %}
                <div class="project-info">
                    <span class="project-category">{{ project.data.category }}</span>
                    <div class="project-text">
                        <span class="project-artist">{{ project.data.artist }}</span>
                        <h3 class="project-title">{{ project.data.title }}</h3>
                        <p class="project-description" data-lang-en>{{ project.data.description }}</p>
                        {% if project.data.description_fr %}
                        <p class="project-description" data-lang-fr style="display: none;">{{ project.data.description_fr }}</p>
                        {% else %}
                        <p class="project-description" data-lang-fr style="display: none;">{{ project.data.description }}</p>
                        {% endif %}
                    </div>
                </div>
            </article>
            {%- endfor -%}
        </div>
        <div class="scroll-arrows">
            <button class="scroll-arrow" id="scroll-left" aria-label="Previous">
                <svg viewBox="0 0 24 24"><polyline points="15 18 9 12 15 6"></polyline></svg>
            </button>
            <button class="scroll-arrow" id="scroll-right" aria-label="Next">
                <svg viewBox="0 0 24 24"><polyline points="9 18 15 12 9 6"></polyline></svg>
            </button>
        </div>
    </div>
</section>

<!-- Press / Clients Section -->
<section class="press-section">
    <p class="press-label" data-i18n="featuredIn">Featured In & Collaborations</p>
    <div class="marquee-wrapper">
        <div class="marquee-track">
            {%- for logo in press.logos -%}
            <img src="{{ logo.image }}" alt="{{ logo.name }}" class="press-logo">
            {%- endfor -%}
            {%- for logo in press.logos -%}
            <img src="{{ logo.image }}" alt="{{ logo.name }}" class="press-logo">
            {%- endfor -%}
        </div>
    </div>
</section>

<!-- About Section -->
<section class="about-section" id="about">
    <div class="about-container">
        <div class="about-image">
            {% if about.image %}
            <img src="{{ about.image }}" alt="Konan David">
            {% else %}
            <img src="/uploads/about.jpg" alt="Konan David">
            {% endif %}
        </div>
        <div class="about-content">
            <!-- English -->
            <div data-lang-en>
                <span class="about-label">{% if about.en.label %}{{ about.en.label }}{% else %}About{% endif %}</span>
                <h2>{% if about.en.title %}{{ about.en.title }}{% else %}Creating powerful visual experiences through movement{% endif %}</h2>
                {% if about.en.p1 %}
                <p>{{ about.en.p1 | safe }}</p>
                {% else %}
                <p>Born in Abidjan, Côte d'Ivoire, and raised in France, Konan David operates at the intersection of urban culture and high-end production. His work spans sold-out arena tours, dance films, music videos, and fashion collaborations with global brands including <strong>Dior</strong>, <strong>Nike</strong>, <strong>Rabanne</strong>, and <strong>Vogue</strong>.</p>
                {% endif %}
                {% if about.en.p2 %}
                <p>{{ about.en.p2 | safe }}</p>
                {% else %}
                <p>Specializing in artist development and movement coaching, Konan empowers performers to unlock their expressive potential. Alongside this expertise, he has worked with renowned artists such as <strong>Gazo</strong>, <strong>Aya Nakamura</strong>, <strong>Bad Bunny</strong>, and <strong>Dadju</strong>.</p>
                {% endif %}
                {% if about.en.p3 %}
                <p>{{ about.en.p3 | safe }}</p>
                {% else %}
                <p>His work is defined by meticulous attention to detail, raw attitude, and effortless flow. Driven by cinematic vision and cultural authenticity, Konan continuously explores new visual languages to elevate every artistic narrative.</p>
                {% endif %}
            </div>
            <!-- French -->
            <div data-lang-fr style="display: none;">
                <span class="about-label">{% if about.fr.label %}{{ about.fr.label }}{% else %}À propos{% endif %}</span>
                <h2>{% if about.fr.title %}{{ about.fr.title }}{% else %}Créer des expériences visuelles puissantes à travers le mouvement{% endif %}</h2>
                {% if about.fr.p1 %}
                <p>{{ about.fr.p1 | safe }}</p>
                {% else %}
                <p>Né à Abidjan, en Côte d'Ivoire, et élevé en France, Konan David évolue à l'intersection de la culture urbaine et de la production haut de gamme. Son travail comprend des tournées à guichets fermés, des films de danse, des clips musicaux et des collaborations mode avec des marques internationales telles que <strong>Dior</strong>, <strong>Nike</strong>, <strong>Rabanne</strong> et <strong>Vogue</strong>.</p>
                {% endif %}
                {% if about.fr.p2 %}
                <p>{{ about.fr.p2 | safe }}</p>
                {% else %}
                <p>Spécialisé dans le développement artistique et le coaching de mouvement, Konan aide les artistes à libérer leur potentiel expressif. Fort de cette expertise, il a travaillé avec des artistes renommés tels que <strong>Gazo</strong>, <strong>Aya Nakamura</strong>, <strong>Bad Bunny</strong> et <strong>Dadju</strong>.</p>
                {% endif %}
                {% if about.fr.p3 %}
                <p>{{ about.fr.p3 | safe }}</p>
                {% else %}
                <p>Son travail se distingue par une attention méticuleuse aux détails, une attitude brute et un flow naturel. Guidé par une vision cinématographique et une authenticité culturelle, Konan explore continuellement de nouveaux langages visuels pour sublimer chaque récit artistique.</p>
                {% endif %}
            </div>
        </div>
    </div>
</section>

<!-- Contact Section -->
<section class="contact-section" id="contact">
    <div data-lang-en>
        <span class="contact-label">{% if contact.en.label %}{{ contact.en.label }}{% else %}Get in Touch{% endif %}</span>
        <h2>{% if contact.en.title %}{{ contact.en.title }}{% else %}Let's create something together{% endif %}</h2>
    </div>
    <div data-lang-fr style="display: none;">
        <span class="contact-label">{% if contact.fr.label %}{{ contact.fr.label }}{% else %}Contact{% endif %}</span>
        <h2>{% if contact.fr.title %}{{ contact.fr.title }}{% else %}Créons quelque chose ensemble{% endif %}</h2>
    </div>
    <a href="mailto:{{ site.email }}" class="contact-email">{{ site.email }}</a>
    <div class="social-links">
        <a href="{{ site.instagram }}" target="_blank">Instagram</a>
        <a href="{{ site.youtube }}" target="_blank">YouTube</a>
    </div>
</section>

<!-- Footer -->
<footer>
    <span data-i18n="copyright">© 2026 Konan David. All rights reserved.</span>
    <span data-i18n="location">Paris, France</span>
</footer>
